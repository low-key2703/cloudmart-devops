apiVersion: v1
kind: Service
metadata:
  name: {{ include "cloudmart.componentName" (dict "component" "user-service" "context" .) }}-service
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "cloudmart.componentLabels" (dict "component" "user-service" "context" .) | nindent 4 }}
spec:
  selector:
    {{- include "cloudmart.componentSelectorLabels" (dict "component" "user-service" "context" .) | nindent 4 }}
  ports:
    - protocol: TCP
      port: {{ .Values.userService.service.port }}
      targetPort: {{ .Values.userService.service.targetPort }}
      {{- if eq .Values.userService.service.type "NodePort" }}
      nodePort: {{ .Values.userService.service.nodePort }}
      {{- end }}
  type: {{ .Values.userService.service.type }}
