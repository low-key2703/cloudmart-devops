apiVersion: v1
kind: Service
metadata:
  name: {{ include "cloudmart.componentName" (dict "component" "product-service" "context" .) }}-service
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "cloudmart.componentLabels" (dict "component" "product-service" "context" .) | nindent 4 }}
spec:
  selector:
    {{- include "cloudmart.componentSelectorLabels" (dict "component" "product-service" "context" .) | nindent 4 }}
  ports:
    - protocol: TCP
      port: {{ .Values.productService.service.port }}
      targetPort: {{ .Values.productService.service.port }}
