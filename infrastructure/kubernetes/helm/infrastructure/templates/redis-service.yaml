apiVersion: v1
kind: Service
metadata:
  name: redis
  labels:
    {{- include "infrastructure.componentLabels" (dict "component" "redis" "context" .) | nindent 4 }}
spec:
  selector:
    {{- include "infrastructure.componentSelectorLabels" (dict "component" "redis" "context" .) | nindent 4 }}
  ports:
    - protocol: TCP
      port: {{ .Values.redis.service.port }}
      targetPort: {{ .Values.redis.service.targetPort }}
